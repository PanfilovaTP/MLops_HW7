upstream model_backend {
    server ml-service-v1:8000 weight=90;
    server ml-service-v2:8000 weight=10;
}

server {
    listen 80;

    location / {
        proxy_pass http://model_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
